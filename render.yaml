services:
  - type: web
    name: assets-pro
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: RESEND_API_KEY
        sync: false
      - key: RECOVERY_FROM_EMAIL
        sync: false
